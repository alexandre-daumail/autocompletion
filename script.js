"use strict";

$(document).ready (event => {

    console.log("ok");

    const villes = [];
    var form = $('form');
    var formDatas = new FormData();
    var input = $('input');
    var resultat = $('ul');
    var lien = $('#a');
    var article = $('article');

    formDatas.append('input', input.value);

})